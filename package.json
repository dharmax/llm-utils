{
  "name": "@dharmax/llm-utils",
  "version": "0.5.0",
  "description": "Small LLM primitives for typed provider execution, structured responses, routing, prompts, context, sessions, and metrics.",
  "type": "module",
  "main": "./dist/index.mjs",
  "types": "./src/index.mts",
  "exports": {
    ".": {
      "types": "./src/index.mts",
      "bun": "./src/index.mts",
      "import": "./dist/index.mjs"
    }
  },
  "files": [
    "dist",
    "src",
    "docs",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "rm -rf dist && esbuild src/index.mts --bundle --platform=node --format=esm --outfile=dist/index.mjs --sourcemap --resolve-extensions=.mts,.mtsx,.js,.json --external:zod --external:node:fs/promises --external:node:fs --external:node:path --external:node:url --external:node:child_process --external:node:util && tsc --emitDeclarationOnly --outDir dist",
    "typecheck": "tsc --noEmit",
    "check": "npm run typecheck && npm run test",
    "test": "npm run build && node --test tests/**/*.test.mjs",
    "test:bun": "bun test"
  },
  "keywords": [
    "llm",
    "ai",
    "routing",
    "prompt-management",
    "session-context"
  ],
  "author": "dharmax",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/dharmax/llm-utils.git"
  },
  "bugs": {
    "url": "https://github.com/dharmax/llm-utils/issues"
  },
  "homepage": "https://github.com/dharmax/llm-utils#readme",
  "dependencies": {
    "@dharmax/pubsub": "file:../pubsub",
    "jsonrepair": "^3.14.1",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "esbuild": "^0.28.0",
    "typescript": "^6.0.3"
  }
}
